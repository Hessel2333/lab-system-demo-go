# 试剂管理系统 — 双 BPM 工作流重构计划

> 文档版本：v1.0 ｜ 创建日期：2026-02-26  
> 背景：现有单一 BPM 流程中，"审批下单"之后采购人员转到外部平台（易派客）操作，系统形成断层。本次重构将业务拆分为两个独立的闭环 BPM，彻底对齐实际工作方式。

---

## 一、核心业务逻辑重设计

### 原有流程（单 BPM）
```
需求申购 → 审批下单 → 到货赋码 → 领用消耗 → 空瓶核销
```

### 重构后（双 BPM）

#### 🔵 BPM-A：需求采购闭环（研发驱动）
```
研发提交申购单
      ↓
采购员任务池：审批 + 在易派客平台下单
      ↓
采购员记录订单凭证（订单号 / 截图）
      ↓
申购状态变更为「采购中」→ BPM-A 闭环 ✅
```

#### 🟠 BPM-B：到货资产流（采购驱动）
```
采购员上传易派客月度/周度采购明细（Excel）
              ↓
      系统 AI 解析，自动匹配申购需求人
      （无法匹配者手动关联，标记「待确认」）
              ↓
         确认到货 → 赋码（UUID 生成）
              ↓
    ┌─────────────────┬──────────────────────┐
    │    普通化学品     │      管控化学品         │
    │                 │                      │
    │  研发自主扫码入库  │  采购专人入管控试剂柜   │
    │  （目标位置：实验室 │  （位置：独立管控区）   │
    │   各房间试剂柜）   │                      │
    └─────────────────┴──────────────────────┘
              ↓
         【领用流程】
     研发提交领用申请单
         团队长审批
              ↓
    ┌─────────────────┬──────────────────────┐
    │    普通试剂      │       管控试剂          │
    │                 │                      │
    │   团队长通过后    │ 团队长通过后 →         │
    │  直接可领取消耗   │ 通知钥匙A/B持有人      │
    │                 │ → 双人在线「确认/驳回」  │
    │                 │ → 全部确认后方可取用     │
    └─────────────────┴──────────────────────┘
              ↓
         扫码消耗 → 空瓶核销
```

---

## 二、数据模型变更计划（后端）

### 新增字段 / 表

#### 1. `ReagentRequest` 表扩展
| 新增字段 | 类型 | 说明 |
|---------|------|------|
| `order_reference` | `string` | 易派客订单号或凭证编号 |
| `order_attachment` | `string` | 订单截图/附件路径 |
| `closed_at` | `datetime` | BPM-A 闭环时间戳 |

#### 2. 新增 `ProcurementBatch`（采购批次导入表）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `uint` | 主键 |
| `uploader_id` | `uint` | 上传人（采购员）|
| `source_file` | `string` | 原始 Excel 文件路径 |
| `period` | `string` | 所属周期（如 `2026-01`）|
| `status` | `string` | `解析中/待确认/已确认` |
| `created_at` | `datetime` | |

#### 3. 新增 `ProcurementBatchItem`（批次明细行）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `uint` | 主键 |
| `batch_id` | `uint` | 关联批次 |
| `reagent_name` | `string` | 原始名称（来自 Excel）|
| `cas_number` | `string` | 解析或手动填写 |
| `quantity` | `int` | 数量 |
| `unit` | `string` | 单位 |
| `unit_price` | `float` | 单价 |
| `matched_catalog_id` | `uint` | 匹配到的品目 ID（可空）|
| `matched_request_id` | `uint` | 匹配到的申购单 ID（可空）|
| `match_status` | `string` | `自动匹配/手动匹配/未匹配` |

#### 4. 新增 `ReagentDispenseRequest`（领用申请表）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `uint` | 主键 |
| `requester_id` | `uint` | 申请领用人 |
| `reagent_item_id` | `uint` | 申请领用的具体单瓶 UUID |
| `amount` | `float` | 领取量 |
| `purpose` | `string` | 领用用途 / 关联实验 |
| `status` | `string` | `待审批/已通过/已驳回/待双签/已完成` |
| `leader_approved_at` | `datetime` | 团队长审批时间 |
| `key_holder_a_id` | `uint` | 钥匙 A 持有人 ID |
| `key_holder_b_id` | `uint` | 钥匙 B 持有人 ID |
| `key_holder_a_confirmed_at` | `datetime` | A 确认时间 |
| `key_holder_b_confirmed_at` | `datetime` | B 确认时间 |
| `expires_at` | `datetime` | 双签超时时间（如 24h）|

---

## 三、API 接口计划（后端）

### BPM-A 相关
| 方法 | 路径 | 描述 |
|------|------|------|
| `POST` | `/api/reagents/requests/:id/approve-and-order` | 审批下单，附上凭证号/截图 |

### BPM-B 相关
| 方法 | 路径 | 描述 |
|------|------|------|
| `POST` | `/api/reagents/procurement-batches` | 上传 Excel，创建新批次 |
| `GET` | `/api/reagents/procurement-batches/:id/items` | 获取批次解析条目列表 |
| `PUT` | `/api/reagents/procurement-batches/:id/items/:item_id` | 手动关联/修正明细行 |
| `POST` | `/api/reagents/procurement-batches/:id/confirm` | 确认批次，触发赋码到货 |

### 领用流程相关
| 方法 | 路径 | 描述 |
|------|------|------|
| `POST` | `/api/reagents/dispense-requests` | 研发提交领用申请 |
| `POST` | `/api/reagents/dispense-requests/:id/leader-approve` | 团队长审批通过/驳回 |
| `POST` | `/api/reagents/dispense-requests/:id/key-holder-confirm` | 钥匙持有人确认/驳回 |

---

## 四、前端 UI 计划

### 4.1 BPM-A：采购闭环的 UI 改造

**改造位置**：`ReagentRequestList.vue` 中采购员操作列

- `审批并下单` 按钮点击后，弹出 **`OrderConfirmDialog`**：
  - 输入框：外部订单号（选填）
  - 附件上传：截图（选填，支持拖拽）
  - 确认后：状态变为`采购中`，BPM-A 完成。

---

### 4.2 BPM-B：到货资产流 UI（全新页面/Tab）

**新增入口**：采购员 Tab 中新增 **「采购明细导入」** 子 Tab

**步骤 1 — 上传 Excel**：
- 拖拽上传区（支持 `.xls` / `.xlsx`）
- 选择所属周期（年月下拉）
- 上传后，系统在后台解析，呈现解析进度动画。

**步骤 2 — 明细确认与匹配**：
- 表格展示解析条目，每行显示：原始名称、数量、单价、匹配状态。
- 🟢 自动匹配成功：显示品目名称及申购人，可点击查看详情。
- 🟡 未匹配/冲突：该行高亮，采购员手动从下拉中选择对应品目/申购人。
- 底部操作：`全部确认并提交到货` 按钮。

**步骤 3 — 到货赋码**：
- 已确认的批次，跳转至到货赋码工作台（现有扫码器页面增强）。
- 普通品→ 生成 UUID 条码可打印，状态标记`已到货，存分拣区`。
- 管控品→ 单独展示，由采购员指定入库到`管控试剂柜`。

---

### 4.3 领用审批与双签 UI

**新增「领用申请」入口**（研发角色，在库存 Tab 每行）：
- 弹出 **`DispenseRequestDialog`**：填写领取量、用途、关联实验。
- 提交后状态变为`待团队长审批`。

**团队长视角**（新增` 领用审批 ` Tab 或推送通知）：
- 展示申请人、试剂名、用量、用途，可通过/驳回（必填驳回原因）。

**管控品双签通知**（钥匙持有人视角）：
- 收到系统消息通知（仪表盘消息墙，或未来可扩展为微信/邮件）。
- 点击「查看详情」弹出确认卡片：显示领用人、用量、用途、有效期。
- 操作按钮：`✅ 确认开锁` / `❌ 驳回（填写原因）`。
- 超时 24h 未操作：自动驳回，通知申请人重新提交。

---

## 五、实施顺序与里程碑

| 阶段 | 工作内容 | 预计工作量 |
|------|---------|----------|
| **P0** | 后端：数据模型迁移（4张新表/字段） | 0.5 天 |
| **P0** | 后端：BPM-A 采购闭环 API | 0.5 天 |
| **P1** | 后端：BPM-B 批次导入解析、明细匹配 API | 1.5 天 |
| **P1** | 后端：领用申请、团队长审批、双签 API | 1 天 |
| **P2** | 前端：BPM-A UI（`OrderConfirmDialog`）| 0.5 天 |
| **P2** | 前端：BPM-B Excel 导入与匹配确认页面 | 1.5 天 |
| **P2** | 前端：领用申请 + 审批 + 双签通知 UI | 1 天 |
| **P3** | 联调测试 + 边界场景处理 | 1 天 |
| **合计** | | **约 7.5 天** |

---

## 六、遗留风险与兜底方案

| 风险点 | 描述 | 兜底方案 |
|--------|------|---------|
| Excel 格式不稳定 | 易派客导出格式可能变化 | 提供字段映射配置界面，允许采购员定义列名 |
| 明细无法自动匹配 | 名称差异导致无法关联申购单 | 允许「无申购单走免审批入库」，走库管直入通道 |
| 双签超时 | 钥匙持有人长时间未响应 | 24h 后自动驳回 + 允许申请人重新发起 |
| 管控品位置追踪 | 管控试剂柜出入记录要求严格 | 所有操作强制写入 `ReagentLog`，审计字段完整 |
