# 更新日志 (Changelog)

记录系统在近期迭代中实施的具体业务逻辑优化与 Bug 修复。

## [2026-02-27] - 全局视角架构收敛（首页承接运营概览）

### ✅ 全局角色上下文已完成

- 新增前端会话 Store：`frontend/src/stores/session.ts`，统一维护 `researcher / procurement / leader` 角色与用户映射。
- 角色选择持久化到 `localStorage`（`lab-system-role`），刷新后保持当前视角。
- `main.ts` 中统一同步 `X-User-ID` 请求头，避免各业务页重复写角色/用户切换逻辑。
- 左侧栏底部角色切换改为全局入口，切换后对全站模块生效。

### ✅ 信息架构调整已完成

- 首页 `Dashboard` 重建为“系统概览”：
  - 按角色显示快捷操作入口（研发/采购/负责人）；
  - 承接原试剂页“运营概览”能力，统一展示关键指标与告警。
- 新增基础数据中心页：`/master-data`，将“试剂品目管理”从试剂业务页拆出至主数据模块。
- 导航新增“基础数据”，并将旧 `materials` 路由重定向到 `/master-data`。

### ✅ 运营概览角色化指标已完成

- `/api/reagents/stats` 支持 `role + user_id` 参数，返回 `role_focus` 角色聚焦指标。
- 研发视角改为“我的申购/我的待入库/我的领用待办/我的在库”个人化统计，不再展示采购治理指标。
- 采购视角改为“待采购申购/已接单待到货/收货待处理/导入待匹配”执行型统计。
- 负责人视角改为“待审批申购/待审批领用/双签进行中/低库存告警”治理型统计。
- 研发视角“近期操作流水”改为仅展示与本人关联记录，避免全局噪音。
- 运营概览布局瘦身为“3核心卡片 + 2图表 + 关键待办/风险摘要”，减少信息堆叠，提升易读性。

### ✅ 试剂页职责边界优化已完成

- 试剂页移除本地“角色切换模拟”与本地用户 ID 映射，改为读取全局会话角色。
- 试剂页移除“运营概览”和“品目管理”Tab，仅保留试剂业务闭环：
  - 申购申请/申购台账
  - 采购导入
  - 到货确认/到货台账
  - 库存台账
  - 领用台账
- 角色差异改为 Tab 可见性控制，不再维护多套页面模板结构。

### ✅ 表格框架统一（第一批）已完成

- 新增统一表格容器组件：`frontend/src/components/ui/TableSection.vue`，标准化“标题/说明/工具条/内容区”结构。
- 全局新增表格规范样式：`apple-table-section-*` 与 `apple-table-empty`，统一间距、文字层级、边框与空态。
- `ReagentRequestList`（申购台账）切换到统一框架，作为标准页面模板。
- `ReagentCatalogManager`（品目台账）切换到统一框架，并复用 `LedgerTable`，与申购台账在布局结构上对齐。
- `ProcurementReceiving`（到货台账）切换到统一框架，统一“搜索左/筛选右”、空态与刷新动作位置。
- `ResearcherArrivalList`（研发到货台账）切换到统一框架，和采购到货台账在头部、工具条与空态语义上保持一致。
- `ReagentUnifiedInventory`（库存台账）切换到统一框架，团队/全库双视图保持同一页面骨架与工具条规范。
- `ReagentDispensePanel`（领用台账）切换到统一框架，研发视角保持“左台账 + 右侧固定申请面板”，采购/负责人视角复用同一流程台账结构。

## [2026-02-27] - 领用审批固定持有人与跨角色台账统一

### ✅ 领用权限与审批链路已完成

- 团队长审批领用不再手工输入 `key_holder_a_id / key_holder_b_id`，改为系统自动分配。
- 双签持有人支持在用户权限中配置：新增 A/B 持有人权限字段与对应接口。
- 新增接口：
  - `GET /api/users/:id/reagent-permissions`
  - `PUT /api/users/:id/reagent-permissions`
  - `GET /api/reagents/dispense-notifications`
- 持有人分配策略升级：
  - 优先读取权限配置；
  - 缺失时走兜底策略（研发团队C团队长 + 采购角色）；
  - 校验 A/B 不能为同一用户。

### ✅ 领用台账跨角色统一已完成

- 研发、采购、团队长统一进入同一“领用台账”入口，角色差异仅体现在可执行动作。
- 研发侧领用页重构为“左侧管控台账 + 右侧固定申请面板”，并支持行内【快捷申请】。
- 领用流程支持统一 BPM 状态展示（提交/审批/双签/完成）与驳回原因回显。
- 新增待办角标联动，Tab 可显示当前角色的领用待处理数量。

### ✅ 权限管理与角色切换体验已完成

- 角色切换模拟改为三按钮直接切换，不再使用下拉菜单。
- 用户权限弹窗新增“试剂双签角色”配置页，可直接配置钥匙持有人 A/B。
- 管控领用页面移除“手持端扫码录入”组件，操作入口统一回归台账。

### ✅ Sprint A 稳定性加固已完成

- `ConfirmProcurementBatch` 改为幂等确认：已确认批次重复点击不再报错，返回友好成功态（含 `already_confirmed` 标记）。
- `UpdateUser` 改为基于字段映射的安全更新，修复 bool/零值字段更新不稳定问题，并补充更新后回读。
- 试剂写操作日志统一记录真实操作者 ID（从请求上下文获取），移除 `UserID=1` 硬编码。
- `StockCheck` 状态口径对齐当前流程状态：
  - 待到货统计改为 `待采购 + 已接单`
  - 待审统计改为 `待审批`
- 新建申购默认申请人改为从请求上下文推断（不再固定 admin）。

### ✅ Sprint B（第一阶段）体验瘦身已完成

- 采购导入页重构为“主路径优先”：
  - 顶部统一摘要（总行数/新增/重复跳过/待处理/可确认）；
  - 默认聚焦异常处理（待处理、已匹配、已忽略、全部）；
  - 手动补录与批量忽略收敛到“高级处理”折叠区，降低主流程认知负担。
- 批次确认前后文案与交互收敛，弱化噪音符号，提示信息更一致。
- 仪器管理页骨架对齐试剂页：统一页头层级与 segmented tab 导航样式，减少模块间 UI 断层。

### ✅ Sprint B（第二阶段）路由治理与性能优化已完成

- 路由占位页治理：`experiments/materials/consumables/polymer/analysis/ai-center` 不再跳转 Dashboard，统一改为“建设中页”并标注模块名称。
- 试剂运营概览文案去噪：去除告警与统计区域的 Emoji 表达，统一为业务文字 + Badge 语义。
- `ReagentManagement` 改为 Tab 级异步组件加载，降低初始包体积：
  - 关键子模块（申购、台账、导入、到货、领用、品目等）全部按需加载；
  - 主包由约 `1043KB` 降至约 `558KB`，进一步拆分后降至约 `11KB`（运营概览图表独立异步 chunk）。

### ✅ 运行稳定性修复（数据库路径）已完成

- SQLite 路径由“当前工作目录相对路径”改为“基于后端源码位置解析的稳定默认路径”，避免从不同目录启动时误读空库。
- 支持通过环境变量 `LAB_DB_PATH` 覆盖数据库路径，便于多环境切换。

## [2026-02-26] - 试剂模块持续优化与稳定性修复（完整落地记录）

### ✅ 导入链路（采购导入）已完成

- 新增导入前重复识别（`dry_run`）：上传后先分析重复条目，再执行正式导入。
- 导入去重升级为“增量导入”：同订单重复上传仅新增未入库行，重复行自动跳过。
- 去重键升级为行级键：`ProcurementBatchItem.RowHash` 入库并参与重复判定（兼容旧数据回退指纹）。
- 导入接口返回统一统计字段：`created_count` / `skipped_count` / `duplicate_in_system` / `duplicate_in_file` / `duplicate_name_hints`。
- `period` 改为可选，支持非整月采购周期导入。
- 导入预览接口已移除，统一并入创建接口的 `dry_run` 模式。
- 前端 Excel 解析增强：新增 `normalizeCell`，兼容对象单元格（避免 `[object Object]` 导致误判重复）。
- 前端上传流程改为“先解析重复提示，再正式导入”，并明确提示“重复将自动跳过”。

### ✅ 批次确认与交互修复已完成

- 修复“有重复即误判不可确认”的前端状态问题，确认按钮不再被重复项误伤。
- 修复“手动匹配后仍不可确认”的交互冲突，已匹配项可继续走确认链路。
- 统一确认失败提示：优先透传后端错误信息，减少泛化“确认失败”。
- 已确认批次在导入页给出明确引导文案：可直接进入到货台账执行后续点验/入库。

### ✅ 到货/入库链路已完成

- 采购点收接口返回本次生成的 `created_uuids`，支持到货后立即打印标签。
- 新增批量标签打印接口：`POST /api/reagents/items/print-labels`。
- 新增研发扫码入库接口：`POST /api/reagents/items/:uuid/check-in`。
- 研发到货台账与扫码页统一改为调用 `check-in` 完成入库。
- 入库约束升级：必须选择“实验室 + 试剂柜”（实验室由试剂柜位置校验）。
- 暂存区命名统一为后端常量：`ReagentStagingArea = "暂存区"`，替换散落字面量。

### ✅ 领用链路与规则统一已完成

- 管控领用页改为“管控试剂台账 + 行内快捷申请”，移除手持端扫码录入组件。
- 普通试剂不再走领用审批，统一走直接消耗/耗尽。
- “耗尽”逻辑统一到单一 API，台账与详情复用同一后端行为。
- 详情页消耗不再固定 1mL，改为可输入并校验。
- 领用申请页补充 BPM 状态展示（提交/审批/双签/完成）。

### ✅ 运营看板与统一体验已完成

- 首页运营概览补充关键指标：`pending_checkin_items`、`pending_receive_lines`、`controlled_in_storage`、`near_expiry_30d`、`active_catalogs`、`consumed_volume_7d`。
- 新增统一操作反馈工具：`useActionFeedback`，并接入核心流程页（到货确认/研发到货等）。
- 试剂模块主导航命名与视觉风格已做第一轮统一（减少噪音符号和不一致标签）。

### ✅ 文档与交付已完成

- 根目录新增并维护系统优化文档：`系统优化意见文档.md`。
- 根目录维护执行清单：`优化实施清单.md`（当前为待办版，仅保留未完成项）。
- 开发文档持续同步 API 与流程变更，便于前后端与测试协作。

## [2026-02-26] - UI 组件标准化重构与全生命周期交互升级

### 🏗️ 视觉规范化 (UI Standardization)

**引入 `class-variance-authority` (CVA) 构建生产级核心组件库**，彻底解决样式硬编码导致的视觉不一致问题：

- **[Button.vue](file:///Users/tian/Codes/lab-system-demo-go/frontend/src/components/ui/Button.vue)**: 支持 5 种具备类型保护的语义化变体 (`primary`, `secondary`, `outline`, `ghost`, `destructive`) 与 3 种大小规格，内置交互微动效。
- **[Badge.vue](file:///Users/tian/Codes/lab-system-demo-go/frontend/src/components/ui/Badge.vue)**: 统一全系统状态标签，支持 `success`, `warning`, `info`, `destructive`, `default` 语义化色彩变体。
- **[Dialog.vue](file:///Users/tian/Codes/lab-system-demo-go/frontend/src/components/ui/Dialog.vue)**: 规范化弹窗容器，支持从 `sm` 到 `full` 的 10 种标准宽度配置，内置平滑入场缩放动画与背景高斯模糊。

### 🚀 业务交互升级 (Features & UX)

- **物资生命周期档案 (Life-cycle Center)**: 深度重构 **[ItemLifecycleDialog.vue](file:///Users/tian/Codes/lab-system-demo-go/frontend/src/components/reagents/ItemLifecycleDialog.vue)**。现在每一瓶试剂都拥有独立的“防伪溯源卡”，集成实物二维码、物理库位坐标、剩余余量动态看板及全量操作流水时间轴。
- **BPM 进度看板规范化**: 统一 **[RequestProgressDialog.vue](file:///Users/tian/Codes/lab-system-demo-go/frontend/src/components/reagents/RequestProgressDialog.vue)** 交互视觉，支持在同一界面查看 BPM-A (采购商下单进度) 与 BPM-B (实物点收/入库进度)，并提供一键批量入库操作。
- **主页大盘重构**: 
    - 采用 **CVA 驱动的胶囊式 Tab 切换**，提升导航反馈感；
    - 关键指标卡片采用更鲜明的配色方案，增强状态提示效果（如红色代表低库存预警）；
    - 操作流水区域逻辑重构，自动按日志类型映射标准 Badge 标签（如“扫码入库”自动标记为 `success`）。

### 🔧 技术债务清理 & 类型安全 (Maintenance)

- **工具链增强**: 引入 `cn` (clsx + tailwind-merge) 工具函数，从根本上解决动态样式合并时的样式覆盖冲突问题。
- **代码净化**: 全量清除此前散落在各业务页面的硬编码颜色逻辑，统一收敛至 UI 组件库管理。
- **类型模型加固**: 清除了全项目所有模板层级的 `Object is possibly 'undefined'` 类型推断风险，通过 `vue-tsc` 构建系统扫描。
- **冗余剔除**: 清理了 20+ 处未使用的 Lucide 图标引入、重复变量定义以及废弃的 CSS 冗余类，显著减小包体积。

---

## [2026-02-26] - 双 BPM 工作流重构与 Excel 采购导入全面升级

### 🏗️ 架构重构 (Architecture)

**正式引入 BPM-A / BPM-B 双通道设计**，将原有的单一采购审批流拆分为两条独立业务闭环：

| 流程 | 业务含义 | 起止节点 |
|---|---|---|
| **BPM-A** 需求采购闭环 | 研发提需 → 团队长审批 → 采购员下单确认 | 申购 → `已接单` |
| **BPM-B** 到货资产流 | Excel 导入 → 智能过滤匹配 → 物理签收 → 入库上架 | CSV 上传 → `已入库` |

### 🚀 新增功能 (Features)

**BPM-A 需求侧**
- 新增「团队长审批」节点 (`POST /api/reagents/requests/:id/leader-approve`)，管控品申购须经团队长审批后方才转采购。
- 采购员「已接单」动作支持填写外部平台订单号（易派客等）及上传截图凭证，实现 BPM-A 采购闭环。
- 申购状态扩充：`待审批` → `待团队长审批` → `待采购` → `已接单`，各角色按角色权限只见自己应操作的按钮。

**BPM-B 到货侧**
- **Excel 智能解析与过滤**：上传易派客明细后，系统自动提取「物资类别」(第22列) 和「商品类别」(第24列)；非试剂耗材（化玻、劳保用品等）在后端直接标记为 `已忽略`，不进入匹配赛道。
- **自动提取采购周期**：从 Excel「下单时间」列（第11列）自动识别并填充批次所属年月（如 `2026-01`），无需手动选择。
- **订单号防重拦截**：读取 Excel 第1列「订单编号」并传至后端，若同一订单已导入则立即拦截并提示，杜绝重复录入。
- **申购单认领 + 空降派库双模式**：明细匹配操作栏的下拉框升级为双分组（`<optgroup>`）：
  - **最佳推荐**：列出系统内「待采购」状态的申购单，按品目+需求人呈现；
  - **直接指派**：若无对应申购单（紧急口头采购），可直接选择研发人员名字进行派发；系统将在后台自动代填一条紧急闭环单据，台账不留缺口。
- **四 Tab 状态工作台**：匹配页面增加「待处理 / 已匹配 / 已忽略 / 全部」标签页过滤，大批量明细一目了然；「一键忽略全部待处理杂项」提供扫尾快捷操作。
- **三段式物流验收**（`ProcurementReceiving.vue`）：明细确认后不再直接生成试剂瓶资产；采购员在独立收货台账页面逐批次物理点验后，系统才正式生成带 UUID 的 `ReagentItem` 实体（进入暂存分拣区 `已到货`），同时自动完成关联申购单的状态闭合。

**领用审批与双人双锁**
- 新增领用申请流程（`POST /api/reagents/dispense-requests`）：研发提交用量/用途申请 → 团队长审批 → 管控品钥匙持有人双签确认/驳回。
- 双签环节设 24h 超时保护，超时自动驳回并通知申请人。

### 🔄 后端数据模型变更 (Schema)

| 表/字段 | 变更类型 | 说明 |
|---|---|---|
| `ReagentRequest` | MODIFY | 新增 `order_reference`, `order_attachment`, `closed_at` |
| `ProcurementBatch` | NEW | 批次导入表，含 `order_number`（唯一索引防重）|
| `ProcurementBatchItem` | NEW | 明细行，含 `match_status`, `receive_status`, `material_category`, `product_category`, `matched_user_id` |
| `ReagentDispenseRequest` | NEW | 领用申请与双签追踪 |

### 🐞 修复与优化 (Fixes & UX)

- 采购审批列表中引入「AI 库存建议」展开行，直接在请购条目内展示当前库存状态与参考建议，辅助采购员决策。
- 品目管理表格恢复「预警线」列可编辑，恢复用户自定义库存预警阈值功能。
- 系统大盘告警区增加独立固高滚动，防止批量告警淹没操作区域。

---

## [2026-02-25] - 试剂柜管理系统深化与交互体验升级

### 🚀 新增功能 (Features)
- **单瓶领用消耗追踪**：后端新增 `/api/reagents/items/:uuid/consume` 接口。并在前端“全库台账”与“团队视图”上为在库试剂增加【使用 (Minus)】操作，支持填写扣减体积与原因，彻底告别粗放式库存管理。
- **空瓶核销机制**：试剂完全耗尽后，增加一键【核销 (Trash)】按钮，触发物理回收流程。试剂在界面上会从“在库”状态剥离。
- **精细化试剂柜管理 Tab**：在“品目管理设置”中心集成柜位管理 CRUD，替代原来的硬编码（C1/C2），为柜子增加了 `Location` (房间号)、`CabinetType` 等物理属性。

### 🔄 核心业务逻辑重构 (Refactoring)
- **管控品智能强制分配**：在“种子数据(Seed)”生成与自动分配逻辑中，实现了合规隔离：
  - **普通化学品**：按研发/分析团队自动进入**本部门各自对应的冷柜（例如 E309）**。
  - **易制毒/易制爆/剧毒品**：无论申购人是谁，一律强制收口并均摊分配至 **F311 共享管控专用柜 (1-4号)**，强制实施双人双锁。
- **关联查询模型对齐**：全量修复库存相关 API，在 GORM 查询层强行插入 `Preload("Cabinet")`，解决由于缺乏对象关联导致前端无法获取物理存放地理信息的严重 Bug。

### 🐞 深度修复与体验优化 (Bug Fixes & UX)
- **【柜里房间两张皮】防呆优化 (Critical)**：
  - **入库拦截**：扫码入库流程中，当操作员选定目标试剂柜后，UI 组件立刻将上方的“存放库位 (Location)”输入框锁定并**强同步**为该柜子的预设房间号，防止手误填入另一个大楼房间。
  - **展示降级保护**：通过引入 `getItemLocation` 函数，实行**“柜位强优先”**显示逻辑。只要试剂挂载了确切的物理柜，前端无视单表数据误差，一律展示柜所在的真实地理房间。
- **有效期兜底逻辑**：新增容错拦截，当扫码设备未获取到有效生产日期（数据为 `0001-01-01`）时，后端将自动赋予 `当前时间 + 1年` 的默认降级有效期，避免前端出现异常红字。
- **展示优化**：
  - 移除了原有的纯 Emoji 试剂柜表征 (`🗄️`) 防止部分浏览器渲染为罕见方块的问题，全部替换为极具视觉冲击力的**纯文本彩色 Tag 体系**（普通柜蓝底蓝字，管控柜红底红字带 `⚠️` 标识）。
  - 修订了所有表格（尤其是全库台账）的默认倒序展示逻辑 (`ORDER BY created_at DESC`)，保障最新入库的心跳数据稳居首位。
  - 动态渲染消耗进度条：根据试剂量 `remaining_volume / capacity` 的比率，使条形图在 `>50%`、`20-50%`、`<20%` 阈值分别呈现 翡翠绿、琥珀黄 和 警报红。
- **测试环境数据净化**：修改 `debug_handler.go` 初始化脚本，自动将开箱演示用的管理员(`admin`)绑定到具体的`DepartmentID: 8`（分析团队）。此举彻底消灭了以往申购的物资进入“未知系统空间”的幽灵数据问题。
