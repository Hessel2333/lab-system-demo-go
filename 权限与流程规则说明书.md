# 权限与流程规则说明书（v1）

日期：2026-02-27  
适用范围：试剂管理模块（申购、到货、库存、领用）+ 组织与供应商主数据  
目标：形成可实现、可审计、可配置的统一权限与流程规则基线

---

## 1. 设计目标

本说明书用于解决以下核心问题：

- 同类流程在不同页面中角色行为不一致
- 权限判断散落在页面和接口中，缺少统一规则
- 缺少“资源-动作-范围”三维授权模型
- 审批/双签/入库等关键动作缺乏标准化审计闭环

输出目标：

1. 定义统一权限模型（RBAC + 数据范围 + 用户例外）  
2. 给出资源-动作矩阵（可直接落表实现）  
3. 给出三条流程链路的状态机与动作授权  
4. 为权限编辑页提供可落地的数据结构与接口草案

---

## 2. 角色模型（Canonical Roles）

系统统一角色（建议）：

- `admin`：系统管理员（全局配置与治理）
- `leader`：团队长（团队审批与人员治理）
- `procurement`：采购人员（采购导入、到货确认、协同跟进）
- `researcher`：研发人员（申购、到货领取、领用申请）
- `director`：负责人（跨团队监督与治理）

历史角色兼容映射（前后端统一口径）：

- `team_leader` -> `leader`
- `member` -> `researcher`
- `procurement_specialist` -> `procurement`

---

## 3. 数据范围模型（Data Scope）

每个权限点除“是否允许”外，还必须附带范围：

- `global`：全局
- `department`：部门级
- `team`：团队级
- `self`：本人数据
- `none`：不允许

范围计算规则（默认）：

1. 用户拥有多个角色时，先合并为“允许范围并集”  
2. 用户级显式禁止（deny）优先于角色允许  
3. 对关键高风险动作可设置“仅单角色生效，不允许并集提升”

---

## 4. 授权决策规则（Policy Engine）

## 4.1 判定顺序

1. 是否存在用户显式禁止（deny）  
2. 是否存在用户显式允许（allow）  
3. 角色默认权限是否允许  
4. 数据范围是否匹配当前对象  
5. 状态机守卫（guard）是否通过

## 4.2 Guard 规则示例

- 到货入库：必须选择实验室和试剂柜
- 管控试剂领用：必须先团队长审批，再双签通过
- 批次确认：至少存在一条“已匹配”明细

---

## 5. 资源-动作矩阵（v1）

## 5.1 资源定义

- `reagent_request`（申购单）
- `procurement_batch`（采购导入批次）
- `reagent_item`（库存实体）
- `dispense_request`（领用申请）
- `catalog_master`（品目主数据）
- `user_org`（用户与组织）
- `supplier_master`（供应商）
- `workflow_meta`（流程配置、待办、通知）

## 5.2 动作定义（标准动作码）

- 通用：`view`, `create`, `edit`, `delete`, `export`
- 审批：`approve`, `reject`
- 采购链路：`import`, `match`, `confirm_batch`, `receive`, `print_label`
- 库存链路：`checkin`, `consume`, `deplete`
- 领用链路：`apply`, `leader_approve`, `keyholder_confirm`
- 治理：`grant_permission`, `manage_master_data`

## 5.3 角色矩阵（摘要）

1. `researcher`
- 申购：`view/create`（`self`）
- 到货：`view/checkin`（`self`）
- 库存：`view`（`team`），`consume/deplete`（自己可操作条目）
- 领用：`apply/view`（`self`）

2. `procurement`
- 申购：`view`（`global`）
- 采购导入：`import/match/confirm_batch/receive/print_label`（`global`）
- 到货：`view`（`global`）
- 领用：`view`（`global`），仅在双签配置下可 `keyholder_confirm`

3. `leader`
- 申购：`approve/reject`（`team`）
- 领用：`leader_approve`（`team`）
- 台账：`view`（`team`）

4. `director`
- 全链路 `view`（`global`）
- 治理审批（可选）：对高风险流程启用二级审批时 `approve`（`global`）

5. `admin`
- 全资源全动作（`global`）
- 包含权限管理、主数据管理、流程配置管理

---

## 6. 流程规则（状态机 + 动作授权）

## 6.1 申购流程（Procurement Request）

状态：

- `draft`（可选）
- `pending_leader`
- `pending_procurement`
- `accepted`
- `rejected`
- `closed`

动作授权：

- `submit`: researcher(self)
- `leader_approve/reject`: leader(team)
- `procurement_accept`: procurement(global)
- `close`: system/admin

通知：

- 提交后通知团队长
- 团队长审批后通知申请人和采购
- 采购接单后通知申请人

## 6.2 到货流程（Arrival / Check-in）

状态：

- `imported`
- `matched`
- `received_labeled`
- `arrived_pending_checkin`
- `in_storage`
- `closed`

动作授权：

- `import/match/confirm_batch/receive/print_label`: procurement(global)
- `checkin`: researcher(self or assignee)
- `override_checkin`（异常）：admin

通知：

- 到货并赋码后通知对应研发
- 超时未入库（>24h）通知研发 + 团队长 + 采购

## 6.3 领用流程（Dispense）

状态：

- `pending_leader`
- `pending_dual_confirm`（管控品）
- `approved_non_controlled`（普通品）
- `completed`
- `rejected`
- `expired`

动作授权：

- `apply`: researcher(self)
- `leader_approve/reject`: leader(team)
- `keyholder_confirm`: configured key holders
- `expire`: system job

通知：

- 提交后通知团队长
- 团队长通过后通知双签持有人
- 完成/驳回通知申请人、团队长、采购（可配置）

---

## 7. 权限编辑页（目标能力）

权限编辑页不再仅做“单字段开关”，需支持三层结构：

1. 角色模板层（Role Template）
- 资源-动作默认权限
- 默认范围

2. 组织范围层（Scope Binding）
- 角色在组织中的生效范围
- 例如 leader 仅在 Team C 生效

3. 用户例外层（User Override）
- 显式允许 / 显式禁止
- 临时授权（生效时间、失效时间）

建议页面布局：

- 左：角色与组织树  
- 中：资源-动作矩阵（批量勾选）  
- 右：用户例外与生效预览（“该用户最终可做什么”）

---

## 8. 数据表建议（后端）

建议补齐以下表（或等效结构）：

- `role_permissions`
  - `role`, `resource`, `action`, `scope`, `effect`
- `user_permission_overrides`
  - `user_id`, `resource`, `action`, `scope`, `effect`, `start_at`, `end_at`
- `role_scope_bindings`
  - `user_id`, `role`, `scope_type`, `scope_id`
- `permission_audit_logs`
  - `operator_id`, `target_user_id`, `before_json`, `after_json`, `reason`

---

## 9. 接口草案（权限管理）

- `GET /api/permissions/roles/:role/template`
- `PUT /api/permissions/roles/:role/template`
- `GET /api/users/:id/permissions/effective`
- `PUT /api/users/:id/permissions/overrides`
- `GET /api/permissions/audit-logs?target_user_id=&date_from=&date_to=`

要求：

- 所有 `PUT` 接口必须写审计日志
- 返回结构包含“最终生效权限”与“冲突解释”

---

## 10. 验收标准（v1）

满足以下条件可进入下一阶段：

1. 三条链路动作均可通过统一权限判定函数校验  
2. 页面只根据“可执行动作列表”渲染按钮，不写硬编码角色判断  
3. 权限编辑后可实时看到“生效结果预览”  
4. 任一关键动作可追溯到“谁授予权限 + 谁执行动作”  
5. 新增角色或资源时，不需要改动多处页面逻辑

---

## 11. 待确认项（需产品/业务最终拍板）

1. `director` 是否具有实际审批动作，还是仅监督查看  
2. 双签持有人是否允许临时替班授权  
3. 部门与团队边界的最终定义（一个用户是否可挂多团队）  
4. 超时策略是否统一为 24h（到货、双签是否可分别配置）  
5. 导出类动作是否默认开放给 leader/procurement

