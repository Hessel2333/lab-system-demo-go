# 实验室系统优化意见文档（基于当前项目完整度）

## 1. 当前完整度判断

当前系统已具备可演示的端到端雏形，但仍处于 **“业务可跑通，规则未收敛”** 阶段。  
核心问题不在于单点 UI，而在于：

- 流程边界（采购/研发/审批）尚未完全固化；
- 数据定义（数量、单位、状态）在多页面有重复与偏差；
- 操作入口分散（台账、弹窗、详情混杂），导致体验和一致性下降；
- 业务动作有“开发视角文案”残留，用户语义不干净。

---

## 2. 你提出的问题（已整理并扩展）

### P0（必须优先改）

1. **流转弹窗出现 `BPM-A / BPM-B` 字样**
- 问题：这是开发术语，不应面向业务用户展示。
- 建议：改为用户语义，如“申购审批流程”“到货与入库流程”。

2. **流转弹窗中保留入库操作，不合理**
- 问题：流程查看与流程执行混在一个弹窗中，职责冲突。
- 建议：流转弹窗只做“只读追踪”；入库动作统一放在“到货台账/扫码台账”。

3. **到货确认缺少“实验室-试剂柜点位”选择**
- 问题：当前“已到货->在库”缺少关键空间归档动作，台账完整性不足。
- 建议：研发入库必须强制选择 `实验室/房间 + 试剂柜 + 具体层位`（至少柜位必填）。

4. **数量、每瓶容量、余量显示存在多处不一致**
- 问题：同一瓶在不同页面显示口径不同，影响可信度。
- 建议：定义统一口径字段（总容量、当前余量、单位、瓶数），前后端统一格式化函数。

5. **“领用申请”应只针对管控试剂**
- 问题：普通试剂也进入审批流，增加无效流程成本。
- 建议：
  - 管控试剂：领用申请 + 审批/双签；
  - 普通试剂：直接消耗（扣减）+ 耗尽核销，无需审批。

6. **“耗尽”动作在台账和详情双入口，逻辑可能分叉**
- 问题：同一业务动作多处实现，容易出现校验差异。
- 建议：统一调用同一个后端动作（同一 API、同一日志、同一状态迁移）。

7. **详情页固定“登记一笔 1mL”不合理**
- 问题：属于开发占位逻辑，不符合实验实际。
- 建议：改为可输入消耗量（带单位和上限校验），支持快捷值但非固定值。

8. **采购导入强制周期、重复文件禁止，限制过强**
- 问题：真实采购常跨月、补录、拆批次，周期不是必填主键。
- 建议：
  - 周期字段改为可选元数据；
  - 支持“同文件增量导入”：通过订单号+行级指纹去重，只新增未入库条目。

9. **同批多瓶（如 5 瓶）应支持一键生成多二维码并有明确提示**
- 问题：当前操作可做，但反馈弱，用户感知不足。
- 建议：点验后弹窗展示“本次生成 N 个二维码”，支持“打印/导出/稍后打印”。

### P1（高优先）

10. **流转弹窗中 A/B 弱绑定策略不清晰**
- 建议：A（审批）与 B（实物）保持“外键关联但状态机解耦”是正确方向；  
  页面应允许“有 A 无 B / 有 B 无 A（紧急直采）”，不能假设必然双向存在。

11. **“到货点验是否需要”应支持模式化**
- 建议：保留点验，但提供两种模式：
  - 标准模式：逐行点验（适合高风险/管控品）；
  - 快速模式：整行收货并批量赋码（适合普通试剂）。

12. **告警数字角标视觉不佳**
- 建议：统一为圆角胶囊或圆点计数，不使用竖向拉伸矩形；并统一到全站 badge 规则。

13. **Tab 命名和信息密度不统一**
- 建议：统一规则：
  - 中文 2-4 字优先；
  - 避免编号、阶段前缀、装饰符；
  - 仅关键导航保留图标，子级 tab 纯文本。

### P2（中优先）

14. **动作入口仍有分散**
- 建议：明确“台账页是主入口，详情页是补充入口”，禁止新增并行入口。

15. **日志语义需要分级**
- 建议：区分系统日志（自动）与用户操作日志（手动），展示时分组，减少噪音。

---

## 3. 额外补充（建议新增）

1. **状态机中心化**
- 把状态定义、可迁移关系、角色权限集中到后端常量，禁止页面自行推断。

2. **单位标准化**
- 建议引入：
  - `package_unit`（包装单位，如 500ml/1kg）
  - `measure_unit`（计量单位，如 ml/g）
  - `capacity`/`remaining` 全部按 `measure_unit` 存储和显示。

3. **导入去重策略**
- 行级唯一键建议：`order_number + reagent_name + spec + supplier + line_no/hash`。
- 支持“预览新增/重复/冲突”三态再提交。

4. **打印闭环**
- 当前“打印”建议升级为：
  - 生成真实二维码内容（UUID + 校验信息）；
  - 批量导出 PDF（标签纸模板）；
  - 记录打印批次号，支持补打与追溯。

5. **权限与身份**
- 目前多处 `UserID=1` / header mock，应尽快接入真实登录态与角色鉴权。

6. **一致性测试**
- 至少增加：
  - 状态迁移测试；
  - 导入去重测试；
  - 并发点验/并发入库测试；
  - UI 台账关键字段快照测试。

---

## 4. 推荐目标流程（简化版）

### 采购
`采购导入 -> 点验(可批量) -> 赋码 -> 打印标签 -> 移交研发`

### 研发
`扫码识别 -> 选择实验室/试剂柜/层位 -> 入库确认 -> 日常消耗 -> 耗尽核销`

### 管控试剂领用
`台账选择目标瓶 -> 发起领用申请 -> 团队长审批 -> 双签 -> 扣减/核销`

---

## 5. 分阶段改造计划

### 阶段 A（1 周，先把链路拉平）

- 去除 BPM 开发术语外露；
- 移除流转弹窗中的执行动作；
- 入库强制柜位选择；
- 统一耗尽/消耗动作 API；
- 普通试剂绕过领用审批。

### 阶段 B（1-2 周，解决数据一致性）

- 数量/容量/单位口径统一；
- 台账字段统一（采购/研发同骨架，仅动作列差异）；
- 导入周期改可选 + 增量导入机制；
- 点验模式（标准/快速）双轨。

### 阶段 C（1 周，体验与稳定性收口）

- 打印模板与批量导出；
- 告警角标/Tab 命名/UI 细节统一；
- 回归测试与操作手册。

---

## 6. 验收标准（建议）

- 用户看不到 `BPM-*` 等开发术语；
- 同一瓶试剂在任意页面的数量/单位/余量一致；
- 采购可一次性生成并打印多瓶二维码，研发可扫码并准确入柜；
- 普通试剂不进入审批流，管控试剂必须经过审批流；
- 状态迁移可追溯、不可越级；
- 重复导入不会报错阻断，能仅增量新增。

---

## 7. 结论

不建议维持现状。建议采用 **“统一台账框架 + 角色化动作链路 + 中心化状态机”** 的方案。  
这样既能满足你提出的操作真实性（赋码、贴码、扫码入柜、管控领用），又能保证界面一致性与系统稳定性。

