# 流程与交互统一框架设计（v1）

日期：2026-02-27  
适用范围：试剂管理全链路（申购、到货、入库、领用）及后续可扩展业务模块  
目标：将“需求提出、需求跟踪、状态显示”抽象为统一可复用框架，减少角色分叉与页面重复建设

---

## 1. 背景与问题定义

当前项目的问题不是单点 UI，而是“同类流程的模型不一致”：

- 相似业务链路（申购、到货、领用）实现方式不同，导致维护成本高
- 角色切换下，页面结构差异大于业务差异，演示和培训成本高
- 状态命名、动作触发、提示语义不统一，用户学习负担重
- 流程日志、待办、通知缺乏统一模型，跨页面追踪困难

结论：需要一个“流程内核 + UI壳层”的统一框架，而不是继续逐页修补。

---

## 2. 总体设计原则

1. 同构优先：同类业务必须复用同一流程骨架  
2. 角色控能力，不控结构：角色差异只影响“可见字段 + 可执行动作”  
3. 状态单一真相：所有状态流转只通过统一状态机驱动  
4. 事件可追溯：每次动作都必须形成标准化事件日志  
5. 页面配置化：表格、动作面板、流程进度条由配置驱动

---

## 3. 统一业务抽象（核心）

将所有链路抽象为 5 类对象：

- `FlowDefinition`：流程定义（节点、状态、动作、角色约束）
- `FlowInstance`：流程实例（一条具体业务单据）
- `FlowTask`：待办任务（当前由谁处理）
- `FlowEvent`：事件流水（谁在何时做了什么）
- `FlowNotification`：通知（谁应被提醒、提醒类型、已读状态）

统一阶段语义：

1. 提出（Create）  
2. 流转（Review/Assign）  
3. 执行（Execute）  
4. 完成（Done）  
5. 异常（Rejected/Expired/Cancelled）

---

## 4. 状态机标准（轻量工作流引擎）

建议先采用“轻量状态机 + 配置驱动”，暂不引入重型 BPM 引擎。

### 4.1 标准状态字段

- `business_type`：`procurement | arrival | dispense`
- `state_code`：机器状态码（统一英文）
- `state_label`：展示状态（中文）
- `version`：乐观锁版本，防并发覆盖
- `updated_by` / `updated_at`

### 4.2 标准动作字段

- `action_code`：动作码（如 `leader_approve`）
- `action_label`：按钮文案
- `allowed_roles`：允许角色
- `guard`：前置条件（如“必须选择试剂柜”）
- `side_effects`：副作用（通知、生成二维码、创建库存实体）

### 4.3 标准流转接口（建议）

- `POST /api/flows/:business_type/:id/actions/:action_code`
- `GET /api/flows/:business_type/:id/timeline`
- `GET /api/flows/tasks?role=&user_id=&business_type=`

这样申购、到货、领用只换配置，不换交互协议。

---

## 5. 三条现有链路的统一映射

## 5.1 申购（Procurement）

- 阶段：提交 -> 团队长审批 -> 采购接单 -> 到货跟踪 -> 完成
- 关键动作：`submit`、`leader_approve`、`procurement_accept`、`mark_arrived`
- 产物：申购单 + 关联到货任务

## 5.2 到货入库（Arrival）

- 阶段：导入 -> 匹配 -> 点收赋码 -> 研发入库 -> 完成
- 关键动作：`import_batch`、`match_item`、`receive_and_label`、`checkin_to_cabinet`
- 产物：库存实体、二维码标签、位置记录

## 5.3 领用审批（Dispense）

- 阶段：提交 -> 团队长审批 -> 双签确认（管控品）-> 完成
- 关键动作：`apply`、`leader_approve`、`keyholder_confirm_a/b`
- 产物：领用流水、消耗记录、通知

统一后效果：三条链路均使用同一“流程详情卡 + 时间轴 + 动作区”组件，仅配置不同。

---

## 6. 统一 UI 框架（在现有 TableSection 基础上扩展）

已完成：`TableSection` 统一外壳。  
下一步建议新增 3 个可复用组件：

1. `FlowStatusPill`：统一状态标签（颜色/图标/语义）
2. `FlowTimeline`：统一流程时间轴（节点、操作者、时间、备注）
3. `FlowActionPanel`：统一动作面板（按角色与状态自动渲染按钮）

页面统一结构：

1. 台账区：搜索、筛选、列表（TableSection）  
2. 详情区：流程节点 + 时间轴  
3. 动作区：当前角色可执行动作  
4. 通知区：关联消息与异常提示

---

## 7. 角色与权限的统一策略

权限应分三层：

1. 角色权限（RBAC）  
2. 组织范围权限（总部/部门/团队/本人）  
3. 用户例外权限（临时授权、特殊禁用）

统一决策规则建议：

- 默认“允许并集 + 显式禁止优先”
- 有效期授权到期自动失效
- 所有权限变更必须产生日志（操作者、变更前后、影响范围）

---

## 8. 可参考的开源思路

按当前项目阶段，建议参考而非直接重依赖接入：

- 状态机建模：XState 思维（前端流程可视化强）
- 轻工作流：Temporal 思维（代码式编排，适合后续异步扩展）
- 重 BPM：Camunda / Flowable（适合后期正式审批平台化）

建议路线：先实现内部轻状态机（v1），保留未来接入外部 BPM 的兼容层。

---

## 9. 迁移路线（建议 4 阶段）

### 阶段 A：模型统一（先后端）

- 抽取统一状态码与动作码枚举
- 建立 `FlowEvent`、`FlowTask`、`FlowNotification` 三表或等效结构
- 为现有申购/到货/领用接口补齐统一动作入口

### 阶段 B：页面统一（再前端）

- 全部台账采用 `TableSection`
- 引入 `FlowTimeline` 与 `FlowActionPanel`
- 角色差异改为配置，不再分叉模板

### 阶段 C：体验统一（联调）

- 统一文案、提示、空态、异常处理
- 统一通知入口与待办计数
- 完成跨角色演示脚本（研发 -> 采购 -> 团队长）

### 阶段 D：治理与交付

- 权限管理页升级为“资源-动作-范围”矩阵
- 输出流程配置手册与演示用例
- 形成可复用模板，推广到耗材/聚合物等模块

---

## 10. 验收标准（Done Definition）

满足以下条件才算“流程统一改造完成”：

- 同类流程页面结构一致，角色仅影响动作和字段
- 三条链路均支持统一时间轴追踪
- 所有状态流转可从事件日志完整回放
- 待办与通知由统一任务模型驱动
- 新增一个业务流程只需“配定义 + 配页面列”，无需重写一套页面

---

## 11. 当前建议执行顺序（下一步）

1. 先产出《权限与流程规则说明书 v1》（资源-动作-范围矩阵）  
2. 抽象后端统一动作入口（至少覆盖申购/到货/领用）  
3. 前端新增 `FlowTimeline` 与 `FlowActionPanel` 两个核心组件  
4. 先在“领用审批链路”试点完整收敛，再复制到申购和到货

