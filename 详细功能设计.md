# 详细功能设计文档 (Detailed Functional Design)

本文档作为《项目架构文档》的补充，专注于定义每个页面、弹窗、表单的具体元素和交互细节。**开发过程中请严格遵循本文档定义的字段和逻辑，以确保与原系统功能的 1:1 还原。**

---

## 1. 仪器管理模块 (Instruments)

**核心路径**: `/instruments`

### 1.1 页面布局
- **Header**:
  - Title: "仪器管理系统"
  - Subtitle: "Instrument Management System"
  - Action Button: "添加仪器" (Icon: Plus, Color: Blue) -> 打开 `AddInstrumentDialog` (占位).

### 1.2 "仪器总览" Tab (Overview) (默认)
- **统计卡片 (4张)**:
  - 布局: 4列网格
  - 卡片1: "设备总数" (Total), Icon: Settings, Color: Blue
  - 卡片2: "可用设备" (Available), Icon: CheckCircle, Color: Green
  - 卡片3: "正在使用" (In Use), Icon: Activity, Color: Purple
  - 卡片4: "故障/维护" (Fault/Maint), Icon: AlertTriangle, Color: Red
- **仪器列表 (Table)**:
  - 列定义: 
    1. **状态 (Status)**: Badge组件 (Available=Green, In Use=Blue, Maintenance=Orange, Fault=Red).
    2. **仪器名称 (Name)**: 主文本显示名称，副文本显示 "品牌/型号".
    3. **实验室位置 (Location)**: 纯文本.
    4. **利用率 (Utilization)**: Progress Bar (0-100%).
    5. **下次维护 (Next Maint)**: 日期格式 (YYYY-MM-DD).
    6. **操作 (Actions)**: 
       - 按钮 "详情" (Variant: Outline) -> 打开 `InstrumentDetailDialog`.
       - 按钮 "状态" (Variant: Ghost/Blue) -> 打开 `StatusUpdateDialog`.

### 1.3 交互详情
#### A. 仪器详情弹窗 (InstrumentDetailDialog)
- **Header**: 仪器名称 + 状态 Badge.
- **Content**:
  - **基本信息区**: ID, 品牌, 型号, 类型, 存放位置, 管理员.
  - **状态历史 (Timeline/List)**: 
    - 展示最近 5 条记录.
    - 每条包含: 状态点 (颜色对应), 时间, 操作人, 备注的原因.
- **Footer**: "关闭" 按钮.

#### B. 状态更新弹窗 (StatusUpdateDialog)
- **Header**: "更新设备状态"
- **Form Fields**:
  1. **当前状态**: 只读文本.
  2. **新状态 (Select)**: 选项 [空闲, 预约中, 使用中, 维护中, 故障, 已报废].
  3. **操作员 (Input)**: 必填.
  4. **备注/原因 (Textarea)**: 选填, Placeholder "请输入状态变更原因...".
- **Footer**: "取消" (Variant: Ghost), "确认更新" (Variant: Solid Blue).
- **逻辑**: 点击确认 -> 调用 `PUT API` -> 关闭弹窗 -> 刷新列表 -> Toast "状态更新成功".

---

## 2. 实验管理模块 (Experiments)

**核心路径**: `/experiments`

### 2.1 "新建委托" Tab (New)
- **功能**: 创建新实验.
- **布局**:
  - **模板选择器 (QuickTemplateSelector)**: 顶部横向滚动或网格, 展示 "常用模板" (如: 拉伸测试, 热重分析).
  - **分步向导 (CommissionWizard)**:
    - **Step 1: 基础信息**: 实验名称 (Input), 优先级 (Select: Normal/High), 完成期限 (Date).
    - **Step 2: 样品信息**: 
      - 样品列表 (支持添加多行).
      - 每行: 样品名称, 材质, 数量.
    - **Step 3: 检测项目**: 复选框组 (Checkboxes) 或 穿梭框 (Transfer).
    - **Step 4: 确认提交**: 展示汇总信息 + "提交委托" 按钮.

### 2.2 "委托管理" Tab (Manage)
- **筛选栏**:
  - 搜索框: Placeholder "搜索实验ID、名称...".
  - 状态筛选: Tag Group [全部, 待审批, 进行中, 已完成].
- **列表项 (List Item)**:
  - 包含: 实验ID, 名称, 申请人, 进度条, 状态Badge.
  - **点击行为**: 点击整行 -> 侧滑或全屏跳转至 `ProcessFlowViewer` (实验流程详情).

---

## 3. 原料管理模块 (Materials)

**核心路径**: `/materials`

### 3.1 元素细节
#### A. 添加原料弹窗 (AddMaterialDialog)
- **Trigger**: Header 上的 "添加原料" 按钮.
- **Form Fields**:
  1. **原料名称 (Input)**: 必填.
  2. **分类 (Select)**: [通用塑料, 工程塑料, 特种塑料, 助剂, 填料].
  3. **规格/牌号 (Input)**: 如 "7042".
  4. **批次号 (Input)**: 自动生成或手动覆写.
  5. **数量 (Input + Unit Select)**: Unit [kg, g, ton].
  6. **供应商 (Select/Combobox)**: 
     - **AI Feature**: 输入名称后, 下方出现 "AI推荐: XX公司 (价格最优)".
- **Footer**: "入库" 按钮.

#### B. 领用原料弹窗 (UsageDialog)
- **Trigger**: 列表项中的 "领用" 按钮.
- **Form Fields**:
  1. **领用数量**: Max 值受限于当前库存.
  2. **用途 (Select)**: [实验消耗, 生产试制, 外借, 其他].
  3. **关联项目 (Input)**: 选填.
  4. **领用人**: 自动填充当前用户.

---

## 4. 试剂管理模块 (Reagents)

**核心路径**: `/reagents`

### 4.1 "试剂库存" Tab
- **列表特殊列**:
  - **CAS No.**: 样式 `font-mono text-xs`.
  - **纯度**: Badge (AR, CP, GR, HPLC).
  - **危化品标记**: 如果是危化品, 显示红色 "危" 图标.

### 4.2 采购申请弹窗 (PurchaseDialog)
- **Form Fields**:
  1. **试剂名称**:.
  2. **CAS号**: 输入 CAS 号可自动填充名称/分子式 (模拟).
  3. **需求量**: .
  4. **供应商**: **AI 推荐区域** (显示3家供应商对比: 价格, 纯度, 货期). 
     - 交互: 点击推荐卡片 -> 自动回填表单.

---

## 5. 耗材管理模块 (Consumables)

**核心路径**: `/consumables`

### 5.1 库存预警逻辑
- **列表显示**: 
  - 当 `CurrentStock <= MinStock` 时, 数量文本变红, 并显示 "补货" 按钮 (Primary Color).
  - 正常状态显示 "申领" 按钮 (Outline).

### 5.2 供应商管理 Tab
- **卡片设计**:
  - 供应商名称, 主营类目.
  - **评分系统**: 5星显示 (Star Icon).
  - 标签: "发货快", "质量好", "价格高".

---

## 6. 聚合物数据库 (Polymer Database)

**核心路径**: `/polymer`

### 6.1 筛选与列表
- **左侧**: 筛选器 (牌号类型, 厂家) + 列表.
- **列表项**: `PolymerGradeCard`
  - 包含: 牌号名, 厂家, 类型, 熔融指数 (MFI), 密度.
  - **Checkbox**: "加入对比".

### 6.2 对比模态框 (ComparisonModal)
- **显示条件**: 选中 2-4 个牌号后点击底部悬浮栏的 "开始对比".
- **内容布局**: 表格形式.
  - 行: 属性 (密度, 拉伸强度, 弯曲模量, 缺口冲击...).
  - 列: 选中的牌号.
  - **高亮**: 每一行中的最优值用绿色背景高亮.
- **Bottom Area**: **AI 分析结论** (一段文本, 分析哪个牌号更适合什么场景).

---

## 7. 基因库分析 (Analysis)

**核心路径**: `/analysis`

### 7.1 性能预测 Tab
- **输入区**:
  - 模型选择 (Dropdown).
  - 参数输入: 基体 (Text), 填料 (Text), 含量 (Number), 温度 (Number).
- **交互动画**:
  - 点击 "预测" -> 按钮进入 Loading 状态 -> 3秒后 -> 结果卡片淡入显示.
- **结果展示**:
  - **置信度**: 环形进度条.
  - **属性列表**: 拉伸强度 (Value + Unit), 弹性模量 (Value + Unit).

---

## 8. 全局通用交互规范

1.  **Loading 状态**: 所有网络请求（表格加载、提交表单）必须有 Loading 状态 (Spinner 或 Skeleton).
2.  **空状态 (Empty State)**: 列表为空时, 展示灰色图标 + "暂无数据" 提示.
3.  **通知 (Toasts)**:
    - 成功: `toast.success("操作成功")` (右上角).
    - 失败: `toast.error("错误信息")`.
4.  **Confirm Dialog**: 删除/敏感操作前必须弹出二次确认框.
