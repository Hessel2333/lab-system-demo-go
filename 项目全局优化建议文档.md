# 项目全局优化建议文档（全量巡检版）

更新时间：2026-02-27

## 1. 当前完整度结论

系统已具备可用的主链路（申购、采购导入、到货、入库、库存、管控领用），整体完整度约 **75%-80%**。  
当前主要矛盾已经从“功能缺失”转为“状态一致性、角色一致性、交互学习成本”。

---

## 2. 已完成项（已勾销）

以下事项在当前代码中已落地，可从待办中移除：

- [x] 试剂模块角色切换改为三按钮直接切换，不再使用下拉选择。  
  位置：`frontend/src/views/ReagentManagement.vue`
- [x] 管控领用页面移除“手持端扫码录入”，改为“台账 + 快捷申请”。  
  位置：`frontend/src/components/reagents/ReagentDispensePanel.vue`
- [x] 研发侧领用改为“左侧台账 + 右侧固定申请面板”，避免上下分散。  
  位置：`frontend/src/components/reagents/ReagentDispensePanel.vue`
- [x] “采购/研发/团队长”统一进入同一领用台账入口（按角色展示不同操作）。  
  位置：`frontend/src/views/ReagentManagement.vue`
- [x] 团队长审批领用时不再手动输入 key holder ID，改为系统自动分配。  
  位置：`backend/internal/handlers/reagent_handler.go`
- [x] 新增“试剂双签角色”权限配置（A/B），支持在用户权限中设置。  
  位置：`frontend/src/components/organization/UserPermissionDialog.vue`、`backend/internal/handlers/organization_handler.go`
- [x] 暂存区命名统一为后端常量 `ReagentStagingArea = "暂存区"`。  
  位置：`backend/internal/handlers/reagent_constants.go`
- [x] 入库点位收敛为“实验室 + 试剂柜”校验链路。  
  位置：`backend/internal/handlers/reagent_handler.go`（`CheckInReagentItem`）

---

## 3. 高优先优化项（P0）

### P0-1 批次确认仍非幂等（会造成“已确认即报错”的负反馈）

- 现状：批次状态非“待确认”时直接返回 `400 Batch already confirmed`。  
  位置：`backend/internal/handlers/reagent_handler.go:1284-1294`
- 风险：用户重复点击或网络重试时被当成失败，感知混乱。
- 建议：改成幂等成功返回（`200` + 已确认提示 + 当前统计），前端不再弹“确认失败”。

### P0-2 用户更新接口存在零值覆盖风险

- 现状：`UpdateUser` 使用 `Updates(models.User{...})`。  
  位置：`backend/internal/handlers/organization_handler.go:138`
- 风险：布尔值/零值字段更新行为不可预测（尤其 A/B 持有人切换）。
- 建议：改为 `map[string]interface{}` 或显式字段白名单更新，并在保存后 `First` 回读返回。

### P0-3 关键业务日志和行为人仍有写死 `UserID=1`

- 现状：入库、打印、消耗、核销等日志多处写死用户。  
  位置：`backend/internal/handlers/reagent_handler.go:714,782,832,906,959`
- 风险：审计不可用，责任追踪失真。
- 建议：统一从鉴权上下文取用户（Header 仅作开发兜底），所有写操作都记录真实操作者。

### P0-4 业务状态枚举不一致（统计与查询口径漂移）

- 现状：`StockCheck` 仍使用旧状态（如“采购中”“待处理”）。  
  位置：`backend/internal/handlers/reagent_handler.go:363,369`
- 风险：统计失真、列表和看板不一致。
- 建议：集中定义状态常量（BPM-A/B 分开），接口统一引用常量，不直接写中文字面量。

### P0-5 真实身份体系未接入，仍依赖“模拟角色 + Header”

- 现状：前端统一写入 `X-User-ID`。  
  位置：`frontend/src/views/ReagentManagement.vue:60`
- 风险：线上不可用、权限边界不可信。
- 建议：接入真实登录态（JWT/Session），后端中间件注入用户身份与角色，页面只读身份不写身份。

---

## 4. 中优先优化项（P1）

### P1-1 导航中仍有多个占位模块指向 Dashboard

- 现状：`experiments/materials/consumables/polymer/analysis/ai-center` 仍为 TODO 占位。  
  位置：`frontend/src/router/index.ts:31-40`
- 建议：统一替换为“建设中页”（含模块说明和计划），避免用户误认功能可用。

### P1-2 采购导入页面认知负担偏高

- 现状：同页包含上传、预检提示、手动追加、匹配、忽略、确认等多种路径。  
  位置：`frontend/src/components/reagents/ProcurementBatchImport.vue`
- 建议：
  - 默认仅显示“异常项”（重复/未匹配）；
  - 高级操作（手动追加、批量忽略）折叠到“高级处理”；
  - 统一顶部摘要文案（总数/新增/重复/忽略/可确认）。

### P1-3 试剂页面与仪器页面设计语言还未完全统一

- 现状：试剂页使用 `apple-*` 风格，仪器页仍是另一套 tab/header 风格。  
  位置：`frontend/src/views/ReagentManagement.vue` vs `frontend/src/views/Instruments.vue`
- 建议：抽出跨模块的页面骨架（标题区、分段 tabs、卡片间距、表格行高、按钮层级）。

### P1-4 文案层面仍有“开发语义”和 emoji 噪音

- 现状：多个卡片/提示使用 emoji 表达状态。  
  位置：`frontend/src/views/ReagentManagement.vue`、`frontend/src/components/reagents/ProcurementBatchImport.vue`
- 建议：状态统一由 Badge + 文本表达，弱化 emoji，提升可读性与专业感。

### P1-5 大组件拆分与性能优化

- 现状：`ReagentManagement.vue` 体量大、分支多，后续维护成本高。
- 建议：按 tab 拆分容器组件 + 懒加载，并补充大表分页/虚拟滚动策略。

---

## 5. 中长期优化项（P2）

- 建立后端“状态迁移守卫层”（统一做合法迁移与角色校验）。
- 为关键查询增加索引策略（`status`、`created_at`、`requester_id`、`matched_*`）。
- 建立 E2E 回归用例：
  - 导入（重复/增量/手动匹配）
  - 到货（部分收货/全收货）
  - 领用（提交/审批/双签/驳回/超时）
- 建立统一错误码与可观测性（请求 ID、业务主键、状态迁移日志）。

---

## 6. 推荐执行顺序（3 个 Sprint）

### Sprint A（本周，先稳逻辑）

- `ConfirmProcurementBatch` 幂等化
- `UpdateUser` 字段更新方式修正
- 写操作日志接入真实用户ID
- `StockCheck` 状态口径与当前状态机对齐

### Sprint B（下周，降学习成本）

- 采购导入页信息架构瘦身
- 试剂/仪器页面骨架统一
- 文案与状态标签统一（去 emoji 噪音）

### Sprint C（第三周，提稳定性）

- 路由占位治理
- 状态机中心化 + 自动化测试补齐
- 关键接口性能与可观测性收口

---

## 7. 验收指标（建议）

- 重复确认导致的“失败提示”归零（幂等后应转为成功提示）。
- 导入流程平均操作步骤下降（目标减少 20%+）。
- 领用流程角色学习成本下降（跨角色页面结构一致）。
- 审计日志可追溯率达到 100%（写操作均有真实用户ID）。

