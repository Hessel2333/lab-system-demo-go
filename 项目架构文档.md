# 实验室系统迁移项目架构文档 (Go + Vue 3)

本文档基于迁移实施计划生成，定义了实验室管理系统从 React 迁移至 Go + Vue 3 后的整体技术架构。

## 1. 文档权威性说明 (Authority of Documentation)

**本项目搭建严格遵循根目录下的以下文档作为唯一真理来源**：

1.  **项目架构文档.md** (本文档): 定义整体技术栈、目录结构和宏观架构。
2.  **详细功能设计.md**: 定义所有模块（包括仪器管理全生命周期重构、试剂智能化增强等）的详细 UI 元素和交互逻辑。

> **注意**: `reference/` 目录下的 React 项目代码仅作为**逻辑参考**和**功能对照**使用。当 reference 与上述根目录文档冲突时，**以根目录文档为准**。

---

## 2. 项目概述 (Project Overview)

本项目旨在构建一个高性能、现代化的实验室管理系统。前端采用 Vue 3 生态重构以提升开发效率和性能，后端采用 Go (Gin) 提供高效稳定的 API 服务。系统设计遵循 Apple Design 风格，强调交互的一致性和视觉的高级感。

**核心设计原则**：
1.  **架构一致性**：保持原有页面结构、Tab页签、导航逻辑不变。
2.  **交互还原**：所有按钮点击、弹窗、跳转逻辑与原设计完全一致。
3.  **UI 统一化**：使用统一的 Vue 组件库（Shadcn-Vue + Tailwind）重构。
4.  **全面覆盖**：覆盖所有 8 个核心模块及 AI 智能中心。

---

## 3. 技术架构 (Technical Architecture)

### 3.1 后端 (Backend)
- **语言**: Go (Golang)
- **Web 框架**: Gin
- **数据库**: **SQLite (默认/推荐)** 或 PostgreSQL / MySQL.
  - *说明*: 为满足 "本地数据库" 需求并降低部署难度，将在项目中集成 GORM + SQLite。数据以文件形式存储在本地 (`lab_system.db`)，开箱即用。
- **配置管理**: Viper
- **API 风格**: RESTful JSON
- **数据策略**: 
  - **全量持久化**: 所有业务数据（仪器、实验、申请单等）均需存入数据库，**严禁在后端使用内存 Mock 数据**。
  - **初始化**: 系统启动时自动迁移 Schema (`AutoMigrate`)，并可选择性预加载种子数据 (Seed Data)。

### 3.2 前端 (Frontend)
- **框架**: Vue 3 (Composition API, `<script setup>`)
- **构建工具**: Vite
- **状态管理**: Pinia
- **路由管理**: Vue Router
- **样式方案**: Tailwind CSS
- **组件库**: Shadcn-Vue (Headless 组件思想)
- **图标库**: Lucide-Vue-Next

---

## 4. 前端架构与设计系统 (Frontend Architecture)

### 4.1 目录结构规划
```
frontend/
├── src/
│   ├── components/
│   │   ├── ui/             # 基础原子组件 (Button, Input, Dialog等)
│   │   ├── dashboard/      # 仪表盘相关组件
│   │   └── modules/        # 各业务模块专用组件
│   ├── views/              # 页面级组件 (Instruments, Experiments等)
│   ├── stores/             # Pinia 状态管理
│   ├── router/             # 路由配置
│   ├── api/                # 后端 API 封装
│   ├── types/              # TypeScript 类型定义
│   └── icons/              # 图标资源
```

### 4.2 统一设计系统 (Design System)
为保证 UI 一致性，定义如下规范：

- **色彩体系**:
    - **Primary**: Blue-600 (主色调), Cyan-500 (辅助)
    - **Functional**: Emerald-600 (成功), Amber-500 (警告), Rose-600 (错误)
    - **Backgrounds**: `bg-gray-50` (App背景), `bg-white` (卡片背景)
- **排版**: Inter 字体, 标题使用 `text-2xl font-bold`, 正文 `text-sm text-gray-600`.
- **组件规范**:
    - 卡片圆角 `rounded-xl`, 默认阴影 `shadow-sm`.
    - 按钮圆角 `rounded-lg`.

### 4.3 核心通用组件
- `PageHeader.vue`: 统一页面头部，支持自定义渐变背景、图标及操作栏。
- `StatCard.vue`: 标准统计卡片，展示数值及趋势。
- `DataTable.vue`: 封装的表格组件，支持分页、排序、筛选。
- `AppDialog.vue`: 统一弹窗容器，规范 Header 和 Footer 样式。

---

## 5. 全局布局 (Global Layout)

**主布局组件**: `MainLayout.vue`

1.  **侧边栏 (Sidebar)**
    - 响应式设计（移动端 Drawer，桌面端固定）。
    - 包含 9 个主要导航项（概览、AI中心、仪器、实验、原料、试剂、耗材、分析、聚合物）。
2.  **概览仪表盘 (Overview Dashboard)**
    - 路由: `/dashboard`
    - 展示核心指标：设备总数、进行中实验、待处理任务。
    - 集成 AI 智能洞察卡片。

---

## 6. 功能模块架构 (Module Architecture)

### 6.1 仪器管理 (Instrument Management)
**依据文档**: `详细功能设计.md` (全生命周期管理)
- **路由**: `/instruments`
- **功能**:
    - **工作台 (Dashboard)**: 待办任务聚合。
    - **仪器台账 (Inventory)**: 现役仪器全列表 (含生命周期状态)。
    - **采购管理 (Procurement)**: 采购申请->验收->入库全流程。
    - **维护保养 (Maintenance)**: 预防性维护与故障报修。
    - **预约使用 (Reservations)**: 可视化日历/周视图预约。
    - **全生命周期档案**: 仪器详情页涵盖从采购到报废的所有历史记录。

### 6.2 实验管理 (Experiment Management)
**依据文档**: `详细功能设计.md`
- **路由**: `/experiments`
- **功能**:
    - **新建**: 快速模板选择，复杂分步表单 (CommissionWizard)。
    - **管理**: 实验列表，状态筛选，流程图视图 (ProcessFlowViewer)。
    - **AI**: 实验排程优化建议。

### 6.3 原料管理 (Material Management)
**依据文档**: `详细功能设计.md`
- **路由**: `/materials`
- **功能**:
    - **库存**: 原料列表，状态标记。
    - **采购**: 添加原料弹窗，集成 AI 供应商推荐与成本优化。
    - **领用**: 领用记录表单。

### 6.4 试剂管理 (Reagent Management)
**依据文档**: `详细功能设计.md`
- **路由**: `/reagents`
- **功能**:
    - **安全**: 危化品分类统计，MSDS 覆盖率。
    - **采购与审批**: AI 辅助采购申请，以及采购审批工作台中的“智能综合判断”（在库、在途、管控属性辅助决策）。
    - **库存精细化**: 单瓶精度消耗追踪，支持品目维度的自定义低库存预警阈值（Alert Threshold）。
    - **列表**: 支持按 CAS 号、化学式搜索。

### 6.5 耗材管理 (Consumable Management)
**依据文档**: `详细功能设计.md`
- **路由**: `/consumables`
- **功能**:
    - **库存预警**: 低库存自动标记。
    - **供应商管理**: 供应商评分卡片。
    - **使用统计**: 部门消耗趋势。

### 6.6 聚合物数据库 (Polymer Database)
**依据文档**: `详细功能设计.md`
- **路由**: `/polymer`
- **功能**:
    - **布局**: 左列表右详情/对比分栏。
    - **对比**: 多选牌号进行参数对比 (PolymerComparisonModal)。
    - **AI**: 材料性能对比分析建议。

### 6.7 基因库分析 (Interactive Analysis)
**依据文档**: `详细功能设计.md`
- **路由**: `/analysis`
- **功能**:
    - **知识图谱**: 可视化展示材料关联。
    - **性能预测**: 选择模型预测材料属性 (AI Mock)。
    - **智能洞察**: 自动生成的分析建议流。

### 6.8 AI 智能中心 (AI Control Center)
- **路由**: `/ai-center`
- **功能**: 全局 AI 助手，聚合各模块的智能推荐能力，提供对话式交互接口。

---

## 7. 后端 API 接口映射 (API Design)

所有前端模块均通过 RESTful API 与 Go 后端通信。

| 模块 | 方法 | 路径 | 描述 |
| :--- | :--- | :--- | :--- |
| **仪器** | GET | `/api/instruments` | 获取仪器列表 (支持筛选) |
| | POST | `/api/instruments` | 新增仪器 |
| | PUT | `/api/instruments/:id/status` | 更新仪器状态 |
| | POST | `/api/instruments/procurement` | 提交仪器采购申请 |
| | POST | `/api/instruments/:id/maintenance` | 提交维护/报修记录 |
| | POST | `/api/instruments/:id/retirement` | 提交报废申请 |
| **实验** | GET | `/api/experiments` | 获取实验列表 |
| | POST | `/api/experiments` | 创建新实验委托 |
| **原料** | POST | `/api/materials` | 添加原料 |
| **试剂** | GET | `/api/reagents/catalogs` | 获取试剂品目库列表及阈值 |
| | GET | `/api/reagents/stock-check` | 查询单品目实时库存、在途数及 AI 审批建议 |
| | PUT | `/api/reagents/items/:uuid/consume` | 单瓶试剂消耗扣减及空瓶核销 |
| | POST | `/api/reagents/requests/:id/approve` | 采购员审批申购单并下单 |
| | POST | `/api/reagents/purchase` | 提交日常采购申请 |
| **分析** | POST | `/api/analysis/predict` | 提交预测请求 (返回模拟AI结果) |
| **统计** | GET | `/api/stats/dashboard` | 获取首页聚合统计数据 |

---

## 8. 实施路线图 (Implementation Roadmap)

1.  **基础设施搭建**: Vue 3 项目初始化，Tailwind 配置，基础组件库构建。
2.  **核心模块迁移 (P0)**: 
    - 仪器管理 (全生命周期实现)。
    - 实验管理。
3.  **次级模块迁移 (P1)**: 原料管理、试剂管理。
4.  **完整功能迁移 (P2)**: 耗材管理、聚合物数据库。
5.  **高阶功能集成 (P3)**: 基因库分析、AI 智能中心、全局 Mock 数据联调。
