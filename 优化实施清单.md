# 优化实施清单（待办）

说明：本清单仅保留未完成项；已完成事项已从本清单移除。

## A. 采购导入链路瘦身（P0）

- [ ] 导入页改为单主路径：`上传 -> 自动识别 -> 处理异常 -> 确认新增`
- [ ] 默认只展示异常项（重复项、未匹配项），已匹配项折叠到“查看全部”
- [ ] “确认入库”按钮语义统一：仅对“新增且待确认批次”可执行，避免“已确认/待确认”混淆文案
- [ ] 重复识别改为行级展示（重复原因：系统重复/文件内重复），并支持按行排除后仅导入新增
- [ ] 上传后结果提示统一：总行数、新增数、重复数、忽略数（同一格式）

## B. 到货与入库一体化（P0）

- [ ] “采购到货台账/研发到货台账”统一为同一台账骨架，仅切换列可见性与操作权限
- [ ] 到货确认后增加一键跳转到“到货台账”并自动带筛选（按批次号）
- [ ] 标签打印升级为真实二维码模板/PDF导出（含批量打印参数）
- [ ] 到货确认、赋码、入库三步状态在同一处可追踪（避免跨页找状态）

## C. 状态机与幂等（P0）

- [ ] `ConfirmProcurementBatch` 改为幂等确认：重复点击返回成功态说明，不再报错中断
- [ ] 批次确认资格由后端统一判定并返回（前端只消费结果，不本地猜测）
- [ ] 手动匹配后实时刷新批次可确认状态（避免“已匹配但不可确认”）
- [ ] 增加状态机单元测试（批次/明细/瓶身三层状态合法迁移）

## D. 信息一致性（P1）

- [ ] 数量、单位、容量、余量统一格式化策略（所有台账+详情+弹窗同口径）
- [ ] 试剂详情中的领用记录改为多笔可追溯，不再固定单笔/固定1mL
- [ ] 管控领用与普通消耗行为说明统一放在页面顶部规则区，减少误操作
- [ ] “暂存区”作为后端常量后，前后端所有展示文案统一引用同一词条

## E. 交互与可用性（P1）

- [ ] Tab 命名收敛为2-4字，不混用图标、数字角标和长文案
- [ ] 系统告警、状态标签、按钮尺寸在试剂模块统一到同一组件规格
- [ ] 关键动作增加二次确认与失败原因透传（确认入库、批量忽略、批量打印）
- [ ] 空状态页补全引导动作（下一步按钮 + 入口链接）

## F. 性能与工程质量（P1）

- [ ] `ReagentManagement` 大包拆分（当前构建产物 >1MB），按 Tab 动态分块加载
- [ ] 表格大数据场景引入虚拟滚动/分页（采购导入、到货台账、库存台账）
- [ ] 前端 `any` 与 `as any` 逐步替换为明确类型，减少运行时歧义
- [ ] 统一错误处理层（HTTP 错误码 -> 中文可执行提示）

## G. 路由与模块边界（P2）

- [ ] 清理路由中 Dashboard 占位 TODO 页面，避免误导航
- [ ] Mock 角色切换从生产视图中下沉到调试开关（仅开发环境可见）
- [ ] 仪器模块与试剂模块共享组件规范（Button/Badge/Dialog/Ledger）并沉淀设计令牌

## H. 文档与验证（P1）

- [ ] 输出接口变更日志（字段变更、兼容策略、前端适配点）
- [ ] 输出 UAT 验收清单（研发/采购/组长三视角关键链路）
- [ ] 为“导入重复识别与增量导入”补充操作手册（含异常案例）
