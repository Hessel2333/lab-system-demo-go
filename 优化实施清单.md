# 优化实施清单（可勾选）

## A. 流程与术语（P0）

- [x] 流转弹窗去除 `BPM-A/B` 开发术语，替换为用户语义文案
- [x] 流转弹窗改为只读，不承载入库执行动作
- [ ] 所有页面排查并移除开发态术语（如 mock/内部阶段名称）

## B. 到货与入库链路（P0）

- [x] 采购点验后返回本次生成 UUID 列表（`created_uuids`）
- [x] 新增批量标签打印接口（`POST /api/reagents/items/print-labels`）
- [x] 采购台账接入“到货确认后打印标签”弹窗流程
- [x] 新增研发扫码入库专用接口（`POST /api/reagents/items/:uuid/check-in`）
- [x] 研发到货台账改为调用 `check-in` 完成入库
- [x] 扫码页入库改为调用 `check-in`
- [x] 入库时强制选择“实验室 + 试剂柜”（实验室由试剂柜自动确定）
- [ ] 打印标签升级为真实二维码模板/PDF 导出

## C. 状态机与一致性（P0）

- [x] 后端增加 ReagentItem 状态迁移校验（禁止越级）
- [x] 入库缺少库位时阻止迁移
- [ ] 将状态机、角色权限抽到统一常量文件（避免 handler 内散落）
- [ ] 增加状态机单元测试（合法/非法迁移）

## D. 台账统一（P1）

- [x] 新增统一台账组件 `LedgerTable`
- [x] 申购台账接入 `LedgerTable`
- [x] 采购到货台账接入 `LedgerTable`
- [x] 研发到货台账接入 `LedgerTable`
- [ ] 全量台账（库存、领用、品目）接入 `LedgerTable`
- [ ] 列字典中心化（按角色仅切换列可见性，不改模板结构）

## E. 领用策略（P1）

- [x] 领用申请入口改为“管控试剂专用”
- [x] 普通试剂改为直接消耗/耗尽，不走审批流
- [x] “耗尽”动作统一到单一 API（台账与详情复用同一逻辑）
- [x] 管控领用页改为“管控试剂台账 + 行内快捷申请”，移除扫码录入组件
- [x] 领用申请页增加 BPM 流转状态展示（提交/审批/双签/完成）
- [x] 详情页移除固定 1mL，改为可输入消耗量并做单位校验

## F. 导入与去重（P1）

- [x] 采购导入中的“周期”改为可选字段
- [x] 同文件重复导入改为“增量导入”（仅新增条目）
- [ ] 设计行级去重键（订单号 + 行哈希）并支持导入预览

## G. 展示与体验（P2）

- [x] 系统告警角标改为统一圆形/胶囊风格（试剂管理页）
- [x] Tab 命名规则固化（2-4字、无编号、无噪音符号，试剂管理主导航）
- [x] 数量/单位/余量显示口径统一（核心台账与详情接入格式化函数）
- [x] 操作反馈统一（核心流程页接入统一 toast + pending 禁用）

## H. 文档与交付

- [x] 根目录输出《系统优化意见文档》
- [x] 开发指南补充状态机与新 API 说明
- [x] 根目录输出《优化实施清单》
- [ ] 输出接口变更日志（供前端/测试同步）
- [ ] 输出 UAT 验收清单（按角色场景）
